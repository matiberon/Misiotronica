import './Header.css';
import SearchBar from '../SearchBar/SearchBar';

const Header = () => {
    return (
        <div className = "header">
        <SearchBar />
        </div>
    )
};

export default Header;